// Evento simple solicitado
const cta = document.getElementById("cta");
if (cta) {
  cta.addEventListener("click", () => {
    // No realiza ninguna accion adicional por requisito
    console.log("CTA presionado");
  });
}
